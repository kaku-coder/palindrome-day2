# palindrome-day2
Day 2 LeetCode 9: Palindrome Number. Check if integer reads same reversed (121→true, 123→false). Reverse digits mathematically, compare with original. O(n) time, O(1) space. JS solution converts to string (121→"121" vs "121") but LeetCode prefers numeric reversal: num % 10, /10 loop
